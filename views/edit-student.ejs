<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Student | Student Result System</title>
    <link rel="stylesheet" href="/admin.css">
</head>

<body>

    <div class="container">

        <!-- NAVBAR -->
        <div class="navbar">
            <h2>Edit Student Details</h2>
            <a href="/admin"><button class="logout-btn">Back</button></a>
        </div>

        <!-- EDIT FORM -->
        <div class="add-student">
            <h3>Editing: <%= student.name %>
            </h3>

            <form action="/edit-student/<%= student.id %>" method="POST" class="add-form">
                <input type="text" name="id" value="<%= student.id %>" placeholder="Student ID" required>
                <input type="text" name="name" value="<%= student.name %>" placeholder="Full Name" required>
                <input type="text" name="rollNo" value="<%= student.rollNo %>" placeholder="Roll No" required>
                <input type="email" name="email" value="<%= student.email %>" placeholder="Email" required>
                <input type="password" name="password" value="<%= student.password %>" placeholder="Password" required>
                <input type="text" name="course" value="<%= student.course %>" placeholder="Course" required>
                <input type="text" name="semester" value="<%= student.semester %>" placeholder="Semester">
                <input type="text" name="department" value="<%= student.department %>" placeholder="Department">
                <input type="number" step="0.01" name="gpa" value="<%= student.gpa %>" placeholder="GPA">
                <input type="text" name="photo" value="<%= student.photo %>" placeholder="Photo URL">

                <select name="role" required>
                    <option value="student" <%=student.role==='student' ? 'selected' : '' %>>Student</option>
                    <option value="admin" <%=student.role==='admin' ? 'selected' : '' %>>Admin</option>
                </select>

                <button type="submit">Save Changes</button>
            </form>
        </div>

        <footer>
            <p>© 2025 | Designed by Mohammad Aadil | Kanpur Institute of Technology, Kanpur</p>
        </footer>
    </div>

</body>

</html>